feat: add MCP tool wrappers for read-only git operations

Add 4 thin @mcp.tool() wrappers in server.py for git_log, git_diff,
git_status, and git_merge_base. Each wrapper validates _project_dir
then delegates to git_operations.read_operations.

- server.py: import and wrap git_log, git_diff, git_status, git_merge_base
- tach.toml: add mcp_workspace.git_operations dependency for server module
- vulture_whitelist.py: register 4 new tool names
- test_server.py: add delegation and no-project-dir tests for each wrapper
