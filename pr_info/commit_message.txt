feat: add pr_mergeable to BranchStatusReport and wire override in orchestrator

- Add pr_mergeable field to BranchStatusReport dataclass
- Wire _apply_pr_merge_override() into collect_branch_status() orchestrator
  to override rebase status when PR is mergeable (squash-merge safe)
- Update _generate_recommendations() to show "Ready to merge (squash-merge safe)"
  when pr_mergeable is True
- Add tests for orchestrator integration path and recommendation variants
