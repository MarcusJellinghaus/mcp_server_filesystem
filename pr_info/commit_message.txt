feat(tree-listing): add deterministic sorting and dirs_only mode

- Sort children dirs alphabetically, then files alphabetically at every
  tree level (dirs always before files)
- Add dirs_only=True rendering: emit directory paths with trailing /,
  skip all file entries
- Add tests for sort order, dirs_only basic/nested/no-files, and
  base_path interaction
