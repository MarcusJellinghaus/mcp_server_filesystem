fix: restore ci_log_parser report building logic

- _find_log_content: add _{job_name}.txt suffix matching as primary
  lookup strategy (Tier 1), keeping existing matching as fallback tiers
- build_ci_error_details: always produce report even without log content,
  include run URL and job URLs, per-job line budget tracking,
  jobs_fetch_warning handling, "(logs not available)" fallback per job
- truncate_ci_details: match p_coder marker format with square brackets,
  add empty input guard
