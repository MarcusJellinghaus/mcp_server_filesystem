Migrate regression test files to new edit_file interface

Migrate three test files to the new edit_file(old_string, new_string)
signature, replacing the old edits-array and dict-return-value patterns:

- test_edit_file_issues.py: Remove tests for deleted features
  (preserve_indentation, snake_case options). Adapt first-occurrence
  test to verify ValueError on multiple matches (new uniqueness check).
  Keep all other regression scenarios with updated assertions.

- test_edit_already_applied_fix.py: Remove preserve_indentation test.
  Adapt all false-positive prevention tests to check string returns
  (diff vs message) instead of dict fields. Keep _is_edit_already_applied
  helper tests unchanged.

- test_edit_file_backslash.py: Adapt to expect ValueError with
  backslash hint instead of error dict.

Part of #124: Redesign edit_file to match Claude Code Edit interface.
