fix: add github_token passthrough to CIResultsManager

Add the github_token parameter to CIResultsManager.__init__ so it is
forwarded to BaseGitHubManager, which already supports it. This allows
callers to provide an explicit token instead of relying solely on config
file lookup.

- Add github_token optional parameter to __init__ signature
- Pass github_token through to super().__init__
- Update docstring Args section
- Add test_github_token_passthrough test
