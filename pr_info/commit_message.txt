feat(git-ops): add read-only git operations module

Add read_operations.py with git_log(), git_diff(), git_status(), and
git_merge_base() functions. All operations:

- Validate args against per-command security allowlists
- Use _safe_repo_context() for Windows handle safety
- Inject --no-ext-diff --no-textconv safety flags (log/diff)
- Support pathspec via separate parameter (-- injected internally)
- Apply output filtering (search) and truncation (max_lines)
- git_diff supports compact mode via render_compact_diff()
- git_merge_base handles --is-ancestor exit code semantics

Add comprehensive integration tests (28 tests) using real git repos
via git_repo_with_commit fixture.
