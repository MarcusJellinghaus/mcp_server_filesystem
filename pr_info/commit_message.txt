feat: add truncation after collapsing in tree listing

Add _truncate function that caps output at 250 entries plus a summary
line when the rendered tree exceeds the threshold. The summary line
reports remaining dirs, files, and total count.

Update list_directory_tree to call _truncate after rendering.
Update existing test for root-only files to expect truncation behavior.
