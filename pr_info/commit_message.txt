feat: add Merge Status line to human and LLM formatters

Add Merge Status display to format_for_human() showing mergeable,
not mergeable (conflicts), or pending states after the PR line.

Add Mergeable= token to format_for_llm() status summary line
when a PR is found.

Merge Status line is only shown when a PR exists (pr_found=True).

Tests cover all merge status states for both formatters plus
omission when no PR is found.
