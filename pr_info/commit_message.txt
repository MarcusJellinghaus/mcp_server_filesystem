feat(server): add search_reference_files tool and update API response

- Add search_reference_files() async MCP tool that delegates to
  search_files_util with reference project's path after ensuring
  availability (same pattern as read_reference_file/list_reference_directory)
- Update get_reference_projects() to return project objects with
  name + url instead of plain name strings
- Update usage string to include search_reference_files()
- Add TestSearchReferenceFiles test class with 5 tests covering
  glob search, pattern search, combined search, not-found project,
  and ensure_available integration
- Update existing get_reference_projects tests for new response format
