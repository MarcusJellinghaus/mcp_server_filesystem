feat: add auto-collapsing for large directory trees

Implement greedy collapse algorithm that reduces trees exceeding 250
lines by collapsing the highest-scoring directories first. Depth-1
directories are protected and never collapsed.

New functions: _count_lines, _recursive_file_count, _score,
_find_collapsible, _collapse. Updated _render to display collapsed
nodes as "dirname/ (N files)" format.

Scoring formula: (direct_files + direct_subdirs * 0.3) * depth
