docs: update CLAUDE.md for unified git tool

Replace 4 individual git tool references (git_log, git_diff,
git_status, git_merge_base) with single unified `mcp__workspace__git`
tool in tool mapping table and git operations section.

Update bash commands list to reflect commands now handled by the
unified git tool (fetch, ls-tree, show moved to MCP).
