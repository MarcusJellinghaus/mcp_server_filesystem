test: add 4 regression tests for merge-base parent detection

Add test cases validating corrected merge-base algorithm behavior:
- test_selects_main_over_dormant_feature_branch: verifies distance
  measured to current HEAD, not candidate HEAD (issue #803 regression)
- test_prefers_default_branch_on_equal_distance: default branch wins
  tiebreaker when distances are equal
- test_includes_candidate_at_threshold: candidate at exactly the
  distance threshold (20) is included via <= comparison
- test_distance_zero_collects_all_candidates: no early-exit at
  distance=0, all candidates collected before tiebreaker applied
