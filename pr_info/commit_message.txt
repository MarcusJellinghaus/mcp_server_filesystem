feat(git): add get_remote_url and clone_repo primitives

Add two git primitive functions to remotes.py:
- get_remote_url(): returns raw origin URL (SSH/HTTPS, any host)
- clone_repo(): full clone with input validation and error wrapping

Export both from git_operations package. Includes unit tests for
all code paths (success, missing origin, non-git dir, mock clone,
validation errors).
