feat(git): support reference_name in git tool

Add optional reference_name parameter to git() MCP tool. When set,
resolves the reference project path via get_reference_project_path()
and runs the git command there instead of the workspace repo.

- Make git() async, drop @log_function_call decorator
- Wrap git_impl in asyncio.to_thread() to avoid blocking event loop
- Add reference_name: Optional[str] = None parameter
- Add 4 tests for git with reference projects
- Update existing git tests for async

Closes #140
