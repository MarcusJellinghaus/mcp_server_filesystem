feat: rewrite reinstall_local.bat to match modern pattern

Full rewrite of tools/reinstall_local.bat following the established
0-6 step template from sibling repos (mcp-coder-utils, mcp-tools-py):

- Use setlocal enabledelayedexpansion + !ERRORLEVEL! for reliable error handling
- Auto-create .venv if missing; deactivate any active venv at start
- Use --python flag on all uv pip commands (no dependency on caller's venv state)
- Add GitHub dependency override step via read_github_deps.py
- Add finalization step (re-run editable install so local source takes precedence)
- Uninstall 5 packages: mcp-workspace, mcp-coder-utils, mcp-config-tool, mcp-tools-py, mcp-coder
- Verify both import (mcp_workspace) and CLI entry point (mcp-workspace.exe)
- Use endlocal & set trick to persist venv activation to caller's shell
