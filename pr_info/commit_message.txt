feat(ref-projects): add ReferenceProject dataclass, URL normalizer and verifier

- Add ReferenceProject dataclass with name, path, and optional url fields
- Add normalize_git_url() for SSH-to-HTTPS conversion, .git stripping,
  trailing slash removal, and host lowercasing
- Add verify_url_match() to compare URLs after normalization
- Add detect_and_verify_url() to detect URL from git repo and/or verify
  against explicit URL, with deferred imports for lazy loading
- Add comprehensive tests for all new functions
