Add infrastructure files: config.py, constants.py, utils/timezone_utils.py

Add three infrastructure modules that github_operations will depend on:

- config.py: get_github_token() and get_test_repo_url() with env var
  fallback to ~/.mcp_coder/config.toml
- constants.py: DUPLICATE_PROTECTION_SECONDS = 60
- utils/timezone_utils.py: utc_now() timezone-aware UTC datetime helper

Include comprehensive tests for all modules (18 new tests).

Part of #104 (step 2/5).
