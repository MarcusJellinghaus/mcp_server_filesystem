Add test_read_operations.py to large-files allowlist

The file-size CI gate fails because tests/git_operations/test_read_operations.py
grew to 963 lines after Step 2 and Step 3 of Issue #198 added integration tests
for non-patch diff flags, --stat -p combinations, and git show prefix
preservation.

Add the file to .large-files-allowlist with a rationale comment, mirroring the
existing precedent for tests/github_operations/test_*.py entries that are
exempted for the same reason: comprehensive integration coverage that is
inherently broad. This preserves the plan's "no files created or deleted"
constraint and keeps the new tests co-located with the rest of the module's
read-operation coverage.
