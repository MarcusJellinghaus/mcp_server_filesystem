feat: add _run_simple_command helper, git_show, and git_branch

- Add _run_simple_command() generic helper for validate→execute→truncate
  pattern (used by fetch, rev_parse, ls_tree, ls_files, ls_remote)
- Add git_show() with compact diff rendering and colon pattern detection
- Add git_branch() with read-only flag enforcement
- Add comprehensive tests: 6 unit tests for _run_simple_command,
  5 integration tests for git_show, 6 integration tests for git_branch
